package org.chees.clean.moving.equation;

import org.chees.clean.board.position.Position;

public class KnightEquation extends LinearEquation{

    public KnightEquation(double a, double b) {
        super(a, b);
    }

    @Override
    public boolean test(Position position) {
        return super.test(position);
    }
}
